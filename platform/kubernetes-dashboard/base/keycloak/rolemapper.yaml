# yaml-language-server: $schema=../../../../schemas/client.keycloak.crossplane.io/rolemapper_v1alpha1.json
apiVersion: client.keycloak.crossplane.io/v1alpha1
kind: RoleMapper
metadata:
  name: kubernetes-dashboard-cluster-admin
spec:
  forProvider:
    realmIdRef:
      name: home
    clientIdRef:
      name: kubernetes-dashboard
    roleIdRef:
      name: kubernetes-cluster-admin
