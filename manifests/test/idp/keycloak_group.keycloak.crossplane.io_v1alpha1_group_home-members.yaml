apiVersion: group.keycloak.crossplane.io/v1alpha1
kind: Group
metadata:
  name: home-members
  namespace: keycloak
spec:
  deletionPolicy: Delete
  forProvider:
    name: Members
    realmIdRef:
      name: home
