apiVersion: v1
kind: Pod
metadata:
  name: test
  namespace: test-sandbox
spec:
  containers:
  - command:
    - tail
    - -f
    - /dev/null
    image: debian
    name: test
  restartPolicy: Always
