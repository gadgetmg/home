apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: keycloak
spec:
  additionalOptions:
  - name: proxy-headers
    value: xforwarded
  db:
    host: keycloak-db-rw
    passwordSecret:
      key: password
      name: keycloak-db-app
    usernameSecret:
      key: username
      name: keycloak-db-app
    vendor: postgres
  hostname:
    adminUrl: https://auth.10.5.0.14.nip.io/
    hostname: auth.10.5.0.14.nip.io
  http:
    httpEnabled: true
  ingress:
    enabled: false
  instances: 1
