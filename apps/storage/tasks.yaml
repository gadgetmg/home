# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:
  kustomize:
    taskfile: ../../taskfiles/kustomize.yaml
    dir: .
    flatten: true
    vars:
      APP: storage
      ENVS_ROOT: .

tasks:
  prereqs:
    deps:
    - :piraeus-operator
