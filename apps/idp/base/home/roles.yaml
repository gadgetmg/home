# yaml-language-server: $schema=../../../../schemas/group.keycloak.m.crossplane.io/roles_v1alpha1.json
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Roles
metadata:
  name: admin-group-roles
spec:
  forProvider:
    groupIdRef:
      name: home-admins
    realmIdRef:
      name: home
    roleIdsRefs:
    - name: argocd-admin
      namespace: argocd
    - name: grafana-admin
      namespace: monitoring
    - name: kubernetes-cluster-admin
      namespace: kube-system
    # roleIdsSelector:
    #   matchLabels:
    #     seigra.net/roles: admin
    #   policy:
    #     resolve: Always
