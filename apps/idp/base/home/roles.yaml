# yaml-language-server: $schema=../../../../schemas/group.keycloak.crossplane.io/roles_v1alpha1.json
apiVersion: group.keycloak.crossplane.io/v1alpha1
kind: Roles
metadata:
  name: admin-group-roles
spec:
  deletionPolicy: Orphan
  forProvider:
    groupIdRef:
      name: home-admins
    realmIdRef:
      name: home
    roleIdsSelector:
      matchLabels:
        seigra.net/roles: admin
