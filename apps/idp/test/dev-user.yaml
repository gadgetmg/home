# yaml-language-server: $schema=../../../schemas/user.keycloak.m.crossplane.io/user_v1alpha1.json
apiVersion: user.keycloak.m.crossplane.io/v1alpha1
kind: User
metadata:
  name: home-dev-user
spec:
  forProvider:
    username: dev
    firstName: Dev
    lastName: User
    email: dev@dev.local
    emailVerified: true
    initialPassword:
    - valueSecretRef:
        name: keycloak-home-dev-user
        key: password
    realmIdRef:
      name: home
---
# yaml-language-server: $schema=../../../schemas/group.keycloak.m.crossplane.io/group_v1alpha1.json
apiVersion: group.keycloak.m.crossplane.io/v1alpha1
kind: Memberships
metadata:
  name: home-dev-user
spec:
  forProvider:
    groupIdRef:
      name: home-admins
    members:
    - dev
    realmIdRef:
      name: home
