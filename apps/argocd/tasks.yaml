# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:
  kustomize:
    taskfile: ../../taskfiles/kustomize.yaml
    dir: .
    flatten: true
    vars:
      APP: argocd
      ENVS_ROOT: .

tasks:
  prereqs:
    deps:
    - :ipam
    - :clusterissuers
    - :kyverno
    - :trust-manager
    - :idp
