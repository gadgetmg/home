resources:
- ../base
- loadbalancer.yaml

helmCharts:
- name: cilium
  repo: https://helm.cilium.io
  releaseName: cilium
  namespace: kube-system
  version: 1.14.5
  additionalValuesFiles:
  - ../base/values.yaml
  - values.yaml

generators:
- secrets-generator.yaml
