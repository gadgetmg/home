# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  default:
    sources:
    - '{{.TASKFILE_DIR}}/**/*'
    - crds/test/*
    - manifests/test/cert-manager/*
    - manifests/test/clusterissuers/*
    - manifests/test/trust-manager/*
    cmd: bats {{.TASKFILE_DIR}}
