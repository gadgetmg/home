# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:
  actual:
    taskfile: ../apps/actual
    internal: true
  ipam:
    taskfile: ../apps/ipam
    internal: true
  cloudflare-gateway:
    taskfile: ../apps/cloudflare-gateway
    internal: true
  clusterissuers:
    taskfile: ../apps/clusterissuers
    internal: true
  trust-manager:
    taskfile: ../apps/trust-manager
    internal: true
  idp:
    taskfile: ../apps/idp
    internal: true
  storage:
    taskfile: ../apps/storage
    internal: true
