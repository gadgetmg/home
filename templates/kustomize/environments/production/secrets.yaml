# yaml-language-server: $schema=../../../../schemas/bitnami.com/sealedsecret_v1alpha1.json
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: APP_NAME
  annotations:
    config.kubernetes.io/local-config: "true"
