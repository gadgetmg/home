# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../base
- secrets/cloudflare-gateway.yaml
- secrets/letsencrypt.yaml
- secrets/linstor.yaml

components:
- ipam
- clusterissuers
- velero
- linstor
- gatewayclasses
