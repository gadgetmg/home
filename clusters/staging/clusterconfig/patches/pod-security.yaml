# yaml-language-server: $schema=https://www.talos.dev/v1.7/schemas/config.schema.json
cluster:
  apiServer:
    admissionControl:
    - name: PodSecurity
      configuration:
        defaults:
          enforce: restricted
          audit: restricted
          warn: restricted
